# Canto Writer

A web app for learning traditional Chinese characters using audio hints in Cantonese.

## TODOs

* Color coding of component cards is not based on purely on component characters, but the stroke "matches" data. This makes it harder to debug incorrect "matches" data.
* Points sound effect is not playing on certain iOS devices (e.g. iphone 12 pro)